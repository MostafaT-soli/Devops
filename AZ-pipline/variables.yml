---
variables:
  VERSION: $(Build.SourceBranchName)_$(Build.BuildId)
  registryConfigName: mostafa-acr
  repoName: micro
  dockerfile: '**/Dockerfile'
  kubernetesServiceConnection: mostafa-aks
  namespace: prod
  containerReg: mostafa.azurecr.io
  imageRepo: micro
  imagePullSecret: azure-acr-secret
